# RMUL 双车比赛快速启动

## 1. 首次或修改后编译

```bash
cd ~/github/RM_simulation_test
set +u
source /opt/ros/humble/setup.bash
source /home/wangxiaotao/ws_livox/install/setup.bash 2>/dev/null || true

export CMAKE_POLICY_VERSION_MINIMUM=3.5
colcon build \
  --symlink-install \
  --packages-up-to rm_combat_gazebo rm_referee \
  --parallel-workers 4 \
  --executor parallel \
  --cmake-args -DCMAKE_POLICY_VERSION_MINIMUM=3.5
```

## 2. 主办方启动正式双车比赛

```bash
cd ~/github/RM_simulation_test
./run_rmul_duel.sh
```

脚本会自动加载 ROS 和本工作区，并默认使用：

- `ROS_DOMAIN_ID=42`
- `ROS_LOCALHOST_ONLY=1`
- 裁判端口 `8765`
- 红方 token `red-test-2026`
- 蓝方 token `blue-test-2026`
- 裁判 token `referee-test-2026`

无界面启动可执行：

```bash
cd ~/github/RM_simulation_test
./run_rmul_duel.sh gui:=false
```

## 3. 裁判端自动测试

该测试会先运行单元测试，再在独立的 ROS 域 `143` 和端口 `18765` 上验证错误 token 拒绝以及 `start/pause/resume/reset`，不会操作正式比赛。

```bash
cd ~/github/RM_simulation_test
./test_referee.sh
```

## 4. 主办方连接交互式裁判 CLI

另开终端：

```bash
cd ~/github/RM_simulation_test
set +u
source /opt/ros/humble/setup.bash
source install/setup.bash

export ROS_DOMAIN_ID=42
export ROS_LOCALHOST_ONLY=1

./install/rm_referee/lib/rm_referee/referee_lan_client \
  --host 127.0.0.1 \
  --port 8765 \
  --role referee \
  --name main-referee \
  --token referee-test-2026
```

交互命令：`start`、`pause`、`resume`、`reset`、`status`、`roster` 和 `quit`。

## 5. 红方选手连接和控制

选手 SSH 登录宿主机后，终端一连接裁判网关：

```bash
cd ~/github/RM_simulation_test
set +u
source /opt/ros/humble/setup.bash
source install/setup.bash

export ROS_DOMAIN_ID=42
export ROS_LOCALHOST_ONLY=1

./install/rm_referee/lib/rm_referee/referee_lan_client \
  --host 127.0.0.1 \
  --port 8765 \
  --role red \
  --name red-player \
  --token red-test-2026 \
  --watch-seconds 7200
```

终端二手动控制红车：

```bash
cd ~/github/RM_simulation_test
set +u
source /opt/ros/humble/setup.bash
source install/setup.bash

export ROS_DOMAIN_ID=42
export ROS_LOCALHOST_ONLY=1

./install/rm_referee/lib/rm_referee/combat_teleop --robot red_robot
```

蓝方把 `red`/`red-player`/`red-test-2026`/`red_robot` 分别换成 `blue`/`blue-player`/`blue-test-2026`/`blue_robot`。

> `combat_teleop` 报 `fire service is unavailable` 时，先确认主办方仿真已完全启动，并确认两个终端的 `ROS_DOMAIN_ID` 都是 `42`。

## 6. 快速发送裁判命令

不进入交互界面，直接执行一次命令：

```bash
cd ~/github/RM_simulation_test
set +u
source /opt/ros/humble/setup.bash
source install/setup.bash

export ROS_DOMAIN_ID=42
export ROS_LOCALHOST_ONLY=1

./install/rm_referee/lib/rm_referee/referee_lan_client \
  --host 127.0.0.1 \
  --port 8765 \
  --role referee \
  --name main-referee \
  --token referee-test-2026 \
  --command start
```

将 `start` 换成 `pause`、`resume` 或 `reset` 即可。
